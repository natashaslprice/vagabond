<div class="container">
	<div class="col-sm-6 col-sm-offset-3">
		<% @posts.each do |p| %>
			<div class="posts">
				<strong><%= p.title %></strong> in <%= p.city %>
				<br>
				<%= p.content %>
				<hr>
			</div>
		<% end %>	
	</div>
</div>

